#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int tableDeMultiplication(int nombre){
   for ( i = 0; i < 10; i++){
      printf(" %d * %d = ", nombre, i);
      return nombre * i;
   }
   
}
